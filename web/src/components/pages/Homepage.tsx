import { MainButton, MainButtonContainer } from '../style/Homepage';

export default () => {
  return (
    <MainButtonContainer>
      <MainButton>
        Students
      </MainButton>
      <MainButton>
        Instructors
      </MainButton>
      <MainButton>
        Classes  
      </MainButton>
    </MainButtonContainer>
  );
};
